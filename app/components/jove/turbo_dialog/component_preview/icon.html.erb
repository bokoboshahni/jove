<%= render(Jove::TurboDialog::Component.new(title: 'Title', icon: 'feather/check-circle', dismissable: true)) do |d| %>
  A dialog is a modal window that appears in front of app content to provide critical information or ask for a decision.

  <% d.action do %><%= render(Jove::Button::Component.new(variant: :text, label: 'Action')) %><% end %>
<% end %>
